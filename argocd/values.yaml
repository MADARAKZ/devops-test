configs:
  cm:
    kustomize.buildOptions: --enable-helm
  params:
    server.insecure: true

server:
  service:
    type: NodePort
    nodePortHttp: 30090

controller:
  replicas: 1

repoServer:
  replicas: 1

applicationSet:
  replicas: 1
