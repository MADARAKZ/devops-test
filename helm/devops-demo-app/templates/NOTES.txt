Release {{ .Release.Name }} deployed to namespace {{ .Release.Namespace }}.

Check status:
  kubectl get pods,svc -n {{ .Release.Namespace }}

Health check:
  kubectl exec deployment/{{ include "devops-demo-app.fullname" . }} -n {{ .Release.Namespace }} -- wget -qO- http://localhost:{{ .Values.app.port }}/
